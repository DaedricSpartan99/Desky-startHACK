import { 
  Box, 
  Image, 
  Badge ,
  HStack
} from '@chakra-ui/react'

import { StarIcon } from '@chakra-ui/icons'

export default function WorkplaceEntry({property}) {
  
  // TODO: detect co-working / farm and other

  return (
    <Box 
      maxW='sm' 
      borderWidth='1px' 
      borderRadius='lg' 
      background='white'
    >
      <HStack>
        <Box 
          p='1'
        >
          <Image borderRadius='10' boxSize='80px' src={property.image} alt={""} />
        </Box>

        <Box p='3' mr='30' ml='10'>
       
          <Box
            mt='1'
            fontWeight='semibold'
            as='h4'
            lineHeight='tight'
            noOfLines={1}
          >
            {property.name}
          </Box>
       
          <Box>
            {property.city}
          </Box>

        </Box>
      </HStack>
    </Box>
  )
}
